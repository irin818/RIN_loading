# Integration Notes

Use this folder for temporary notes, prototypes, and adapter mapping records before changes move into RIN's `src/` tree.

Production code belongs in `src/`; exported model assets may later be copied or referenced from the app's public asset path once the loading strategy is chosen.
